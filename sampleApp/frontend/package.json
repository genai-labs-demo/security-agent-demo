{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Static frontend for AWS Security Agent demo application",
  "scripts": {
    "build": "echo 'Frontend uses vanilla HTML/CSS/JS - no build step required'",
    "deploy": "./deploy.sh",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "jest-environment-jsdom": "^29.5.0"
  }
}
